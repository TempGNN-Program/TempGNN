# Paper Figure Reference Inputs

`paper_figure_values.csv` is the sole numeric input used by
`python3 -m scripts.reproduce_paper_figures`. It is an anonymized transcription
of the accepted-paper plotting sources. It is not output from the fresh U280
measurement workflow.

The reference figures and fresh measurements are deliberately separate:

- `results/paper_reproduction/`: deterministic regeneration of paper-reference
  CSV/SVG files from this table.
- `results/reviewer_u280_runs/<run-id>/`: rows freshly emitted by four distinct
  U280 xclbins, including board logs, checksums, power samples, and provenance.

## Source Classes

| Source ID | Input form | SHA-256 | Figures |
| --- | --- | --- | --- |
| `workbook_a` | Author plotting workbook, exact numeric cells | `b1435a8afb1f73b7049922076ab87c8a0d3822f6c7372ccb67044f153784177f` | Fig.2 |
| `workbook_b` | Author plotting workbook, exact numeric cells | `fb9c95a3e356b9e2c601c612941d7e66ee9c3fa7d1b92392c77ac57fa3d768cf` | Fig.9(b), Fig.10, Fig.13 |
| `vector_fig4` | Author vector export, axis-calibrated geometry | `c97190cf681525f096318bf78da17ddedc9d0c440b8544665953c792405acf5f2` | Fig.4(a) |
| `vector_fig11` | Author vector export, axis-calibrated geometry | `e2f655b815ea93b42fd8b4d07a3338108b05d526538a508a06911ee8a44f9699` | Fig.11 |
| `vector_fig12` | Author vector export, broken-axis-calibrated geometry | `03631bb354c124578afb69b94bca09c584300e28db6692d3f08578ea1bbc7051` | Fig.12 |
| `vector_fig14` | Author vector export, axis-calibrated marker geometry | `7899270127476690fd8215bb6c8a7fc329563deca39c9a447654f1c5d9018d70` | Fig.14 |

The source workbooks and vector exports are not included because their file
metadata belongs to the non-anonymous author workspace. The anonymized table
retains cell/geometry locators, source hashes, extraction class, and uncertainty
notes so every packaged value has an auditable origin.

## Transcription Notes

- Fig.10 and Fig.13 use exact workbook cells. Their model blocks follow the
  published axis order `JODIE`, `TGN`, `TGAT`, `APAN`. Two labels in a
  neighboring workbook block are swapped; the numeric block order is confirmed
  by the published vector figure.
- Fig.11, Fig.12, and Fig.14 did not have a separate numeric table in the
  recovered author directory. Their values are digitized from vector geometry,
  not synthesized from prose averages.
- The explicit Fig.11 AVG bar digitizes to `7.7889` for TempGNN/MATG, while the
  paper narrative reports `7.6x`. This package preserves the plotted source and
  records the discrepancy instead of forcing either value.
- Explicit AVG cells/bars are retained as source rows. They are not silently
  replaced by an average of rounded per-point values.

No value in this table is generated by interpolation from a reported mean or
range. The former `_scaled_grid` construction and its synthetic motivation data
were removed.
